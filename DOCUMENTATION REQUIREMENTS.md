Documentation and implementation requirements:

- Write a comprehensive Markdown document for the pipeline.
- The Markdown document must explain:
  - the purpose of the pipeline
  - the end-to-end workflow
  - each pipeline stage and its role
  - the APIs and tools used
  - the source verification logic
  - the JSON output schema
  - failure cases, uncertainty handling, and limitations
  - suggested future improvements

- Generate readable and modular implementation code.
- Add clear comments and docstrings throughout the code.
- Each major function should explain:
  - what it does
  - which pipeline stage it belongs to
  - why it is needed
- Important decision logic, especially source verification and candidate ranking, must be explicitly commented.
- The code should be easy for another researcher to understand, debug, and extend.
